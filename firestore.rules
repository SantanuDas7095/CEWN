
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    function isAdmin() {
      return request.auth != null && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }
    
    match /userProfile/{userId} {
      allow get, update, create: if isOwner(userId);
      allow list: if isAdmin();
      
      match /nutritionLogs/{logId} {
        allow read, write: if isOwner(userId);
      }
    }
    
    match /roles_admin/{userId} {
      allow get: if isOwner(userId) || isAdmin();
      allow list: if isAdmin();
    }

    match /emergencyReports/{reportId} {
      allow create: if request.auth != null;
      allow read, list: if isAdmin();
    }

    match /hospitalFeedbacks/{feedbackId} {
      allow create: if request.auth != null;
      allow get: if isOwner(request.resource.data.studentId);
      allow list: if request.auth != null;
    }

    match /messFoodRatings/{ratingId} {
      allow create: if request.auth != null;
      allow get, list: if true;
    }
    
    match /appointments/{appointmentId} {
       allow create: if request.auth != null;
       allow read: if isAdmin() || isOwner(resource.data.studentId);
       // Allow admins to list by date, and users to list their own.
       allow list: if isAdmin() || (request.auth != null && request.query.where.studentId == request.auth.uid);
       allow update: if isAdmin() || (isOwner(resource.data.studentId) && request.resource.data.status == 'cancelled' && resource.data.status == 'scheduled');
    }

    match /campusInfo/hospital {
      allow get: if true;
      allow write: if isAdmin();
    }
  }
}
